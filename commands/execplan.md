---
description: Draft an ExecPlan for the work just discussed
argument-hint: [optional focus or scope note]
---

Create an ExecPlan for what we have discussed. #$ARGUMENTS

Locate the methodology before writing anything, and use the first of these that exists:

1. `docs/PLANS.md` in this repository
2. `_docs/PLANS.md` in this repository
3. `~/.claude/docs/PLANS.md` — the canonical copy, deployed from the `Taka499/claude` config repo

Read that file in full first: it is non-negotiable that the plan follows it to the letter, and its skeleton (`Purpose / Big Picture`, `Progress`, `Surprises & Discoveries`, `Decision Log`, `Outcomes & Retrospective`, plus the context/steps/validation sections) is what makes a plan resumable by a session with no memory of this conversation.

Write the plan where this repository already keeps them — `docs/plans/`, `_docs/`, or whatever pattern its existing plan files follow. If there are none yet, ask where it should live rather than inventing a layout.

Reference the methodology file you used by its repository-relative path in the plan's opening lines, so a later session knows which rules the document was written against.
